/* @ds-bundle: {"namespace":"RentalWrangler","components":[],"sourceHashes":{},"inlinedExternals":[]} */
(function () {
  // Rental Wrangler is a CSS/token design system — no importable React components.
  // Designs are built from styles.css (tokens + component classes) and the per-component docs.
  window.RentalWrangler = window.RentalWrangler || {};
})();
