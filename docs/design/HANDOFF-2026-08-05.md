# Handoff — 2026-08-05

**Branch:** `claude/design-system-phase-1-vcgald` · **PR #798**, open, 86 commits, unmerged.
`trunk` and `production` are identical — nothing from this session is live, by choice.

## Start here

1. Read the **`art-pipeline`** skill. It is the method, and it is what next session tests.
2. Read **`docs/design/NEXT-SESSION-PROMPT.md`** — paste the block below its rule as the
   opening message. Jac's decision: **test the method on ONE component first**, timed, before
   applying it more widely.
3. Ledger rows **#238–#260** cover everything settled today.

## What shipped to the branch

| | |
|---|---|
| `.claude/skills/art-pipeline/` | The method: export from Figma, never recreate; shared assets; 9-slice; stretch vs round; eight traps |
| `docs/design/SLICE-SPEC.md` | Slice bands per panel, measured and adversarially verified, with paste-ready CSS |
| `docs/design/v2-card/` | The Figma card `438:274` running as live HTML/CSS. Serve the folder, open `card.html` |
| `docs/design/NEXT-SESSION-PROMPT.md` | The timed test of the method |
| `.claude/settings.json` | +27 permission entries (see below) |
| Ledger #238–#260 | Every decision, including two of my own errors scoped back |
| `MEMORY.md` | Decisions, gotchas and open threads appended |

## State of the V2 card

Jac's verdict: **"it's a start."** Not at fidelity.

Working: measured coordinate composition (not flex — a flex approximation was built and
rejected); assets pass the tone gate; rows fused to their elbows (#256), hiding behind the
group header and riding the channel down as it reveals; per-state lasers red/blue/yellow with
steel never tinting; `LayoutCount: 0` at ~60fps across 96 elbows.

Not done: the build still uses fixed-size scaled art, **not** `border-image` 9-slice. The
spec exists; nothing is wired to it yet.

## Blocked on Jac's Figma work — do not try to fix these in CSS

- **`asm-deck` cannot be 9-sliced below 1316px.** Chips, nameplate and "PROMISED" are frozen
  in a 1100px fixed corner; the widest uniform corridor in the whole panel is 19px. Fix is to
  pull those elements out of the art into live DOM.
- **`asm-housing`'s interior staircase is a NEAR-rhythm** (pitch 460/440/459) — it can neither
  stretch nor `round`. Decided fix: move all diagonals and jogs into the end caps.
- **`asm-headboard` / `asm-rowboard`** need text-free plate re-exports.
- **The rail export has three defects** (#259): a 1px transparent slot at x=19, a right edge
  clipped mid-ramp, and an accent that is RED not the canon `#ff7e1f`. Author it neutral and
  tint per state.
- **The slice corridors need named spacer frames** — the deck's has 5px and 4px of slack.

Panels that slice cleanly today and need no redraw: **headboard, rowboard, channel.** Start
the timed test on one of those.

## Permissions

27 entries added to `.claude/settings.json`, committed, so they load at session start.
`python3`/`curl`/`cp`/`mv` were already allowed — the prompts came from the file-plumbing set
(`base64`, `md5sum`, `file`, `stat`, `chmod`, `tar`), dev-server lifecycle (`pkill`, `lsof`,
`kill`), and seven Figma tools including `download_assets` and `read_skill_uri`.
Still prompting by design: `rm`, `git reset`, `git rebase`, force-push.

## Mistakes worth not repeating

- I pixel-diffed exported assets against Figma PNGs and treated mismatches as failures. Two of
  those "failures" were a **uint8 overflow** in my own comparison turning Δ=1 into Δ=36. Two
  agent passes were wasted chasing tones that already matched. **The exported asset is ground
  truth.**
- I split geometry and assets across two agents, so nobody owned the composition — that
  produced the wrong-layout rebuild Jac rejected. **One agent owns both.**
- I tried hand-rebuilding the elbow in CSS before trying Figma's own SVG export.
- I escalated a CVD finding into a blocking gate on a component whose behaviour had not
  changed, and kept raising it after Jac had settled it.
