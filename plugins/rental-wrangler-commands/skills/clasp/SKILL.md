---
name: clasp
description: Prepare an additive Google Apps Script backend deployment for Rental Wrangler.
---

# Clasp

Follow the `Backend push runbook` in `AGENTS.md`. Use the service-account path, pull the live backend, prepare only an additive diff, show Jac that diff, and wait for confirmation before pushing. Never use interactive clasp OAuth as the default path and never commit backend files.
